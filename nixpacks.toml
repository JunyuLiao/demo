[phases.setup]
nixPkgs = ["gcc", "glpk", "python39", "python39Packages.pip"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "make web-real"
]

[phases.build]
cmds = [
    "echo 'Build completed'"
]

[start]
cmd = "python3 web_app_simple.py"
